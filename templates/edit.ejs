<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>

<body>
    <h1>Edit Link</h1>
    <form action="/edit/<%=body.id%>" method="POST">
        <input class="field" type="text" name="title" id="title" placeholder="Título" value="<%=body.title%>" required>
        <input class="field" type="text" name="description" id="description" placeholder="Descrição"
            value="<%=body.description%>">
        <input class="field" type="text" name="url" id="url" placeholder="URL" value="<%=body.url%>" required>
        <button> Salvar</button>
    </form>

    <%if (error) {%>
        <h3>
            <%= error.message%>
        </h3>
        <% } %>


</body>

</html>